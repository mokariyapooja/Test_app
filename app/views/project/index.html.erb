<h1><%= @title %></h1>
<h1>Listing Project</h1>
  <div>
    <% @project.each do | p| %>
       <%= p.name %> 
        <%= link_to p.name, project_path(current_user) %>
         
    <% end %>
  </div>